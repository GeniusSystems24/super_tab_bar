name: super_tab_bar_example
description: Runnable examples for the super_tab_bar package.
publish_to: "none"
version: 1.0.0

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  super_tab_bar:
    path: ../

flutter:
  uses-material-design: true
